# 🔧 Исправление: Default nginx перехватывает запросы

## Проблема
- `curl http://83.217.208.93` возвращает дефолтную страницу nginx
- Это значит, что default конфигурация перехватывает запросы
- Наша конфигурация efflow-m50.com не используется

## Решение на сервере

### 1. Удалите default конфигурацию:
```bash
rm /etc/nginx/sites-enabled/default
```

### 2. Проверьте, что осталась только наша конфигурация:
```bash
ls -la /etc/nginx/sites-enabled/
# Должен быть только efflow-m50.com
```

### 3. Проверьте синтаксис:
```bash
nginx -t
```

### 4. Перезагрузите nginx:
```bash
systemctl reload nginx
```

### 5. Проверьте работу:
```bash
# По IP (должен вернуть проксированный контент)
curl http://83.217.208.93

# По домену (должен вернуть проксированный контент)
curl http://efflow-m50.com
```

---

## Если default нужен для других сайтов

Вместо удаления, отредактируйте default конфигурацию:

```bash
nano /etc/nginx/sites-available/default
```

Измените `server_name` на что-то другое (например, `_` или `localhost`), чтобы он не перехватывал запросы к efflow-m50.com.

---

## Проверка после исправления

```bash
# Должен вернуть HTML от eflow.ie (через прокси)
curl http://83.217.208.93

# Должен вернуть HTML от eflow.ie (через прокси)
curl http://efflow-m50.com
```
