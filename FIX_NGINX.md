# 🔧 Исправление проблемы Connection Refused

## Проблема
- Nginx слушает на `0.0.0.0:80` ✓
- Конфигурация правильная ✓
- Но `curl http://efflow-m50.com` получает "Connection refused"

## Причина
Возможно конфликт с `default` конфигурацией nginx или проблема с сетевой маршрутизацией.

## Решение на сервере

### 1. Проверьте default конфигурацию:
```bash
cat /etc/nginx/sites-enabled/default
```

Если она слушает на порту 80 и конфликтует - удалите её:
```bash
rm /etc/nginx/sites-enabled/default
```

### 2. Проверьте основной nginx.conf:
```bash
cat /etc/nginx/nginx.conf | grep -A 5 "include"
```

### 3. Проверьте, нет ли других server блоков на порту 80:
```bash
nginx -T | grep "listen 80"
```

### 4. Проверьте логи nginx при запросе:
```bash
# В одном терминале:
tail -f /var/log/nginx/error.log

# В другом терминале:
curl -v http://efflow-m50.com

# Смотрите что появляется в логах
```

### 5. Попробуйте подключиться напрямую к IP:
```bash
curl -v http://83.217.208.92
# Должен работать, если nginx правильно настроен
```

### 6. Проверьте сетевую маршрутизацию:
```bash
# Проверьте, может ли сервер подключиться к самому себе по IP 92
curl -v http://83.217.208.92

# Если не работает - проблема в сетевой конфигурации
```

### 7. Если default конфликтует, удалите её и перезагрузите:
```bash
rm /etc/nginx/sites-enabled/default
nginx -t
systemctl reload nginx
```

### 8. Проверьте, что nginx действительно принимает соединения:
```bash
# Проверьте с локального IP
curl -v http://127.0.0.1

# Должен вернуть ответ от nginx
```

---

## Если ничего не помогает

Попробуйте явно указать IP в конфигурации:

```bash
# Отредактируйте конфигурацию
nano /etc/nginx/sites-available/efflow-m50.com

# Измените:
# listen 80;
# на:
# listen 83.217.208.92:80;

# Проверьте синтаксис
nginx -t

# Перезагрузите
systemctl reload nginx
```
