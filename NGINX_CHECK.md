# 🔍 Диагностика nginx для efflow-m50.com

## Проблема
Сайт работает локально (`curl http://127.0.0.1:3000`), но не работает через домен (`efflow-m50.com`).

## Команды для диагностики на сервере

### 1. Проверьте, что nginx использует правильную конфигурацию:

```bash
# Проверьте, есть ли конфигурация в sites-enabled
ls -la /etc/nginx/sites-enabled/

# Должен быть симлинк на efflow-m50.com

# Проверьте содержимое конфигурации
cat /etc/nginx/sites-enabled/efflow-m50.com

# Или если используется другой путь:
cat /etc/nginx/sites-available/efflow-m50.com
```

### 2. Проверьте, что nginx видит конфигурацию:

```bash
# Проверьте все server блоки для efflow-m50.com
nginx -T | grep -A 50 "server_name efflow-m50.com"

# Должен показать ваш server блок с proxy_pass
```

### 3. Проверьте логи nginx в реальном времени:

```bash
# Откройте логи ошибок
tail -f /var/log/nginx/efflow-m50-error.log

# В другом терминале попробуйте открыть сайт
curl -v http://efflow-m50.com

# Смотрите что появляется в логах
```

### 4. Проверьте логи доступа:

```bash
tail -20 /var/log/nginx/efflow-m50-access.log
```

### 5. Проверьте статус nginx:

```bash
systemctl status nginx
```

### 6. Проверьте синтаксис конфигурации:

```bash
nginx -t
```

### 7. Проверьте, слушает ли nginx на порту 80:

```bash
ss -tlnp | grep :80
# Должно показать nginx слушающий на 0.0.0.0:80
```

### 8. Проверьте файрвол:

```bash
# Ubuntu/Debian
ufw status

# Если файрвол активен, разрешите порт 80
ufw allow 80/tcp
```

### 9. Проверьте, может ли nginx подключиться к Node.js:

```bash
# От имени nginx пользователя (обычно www-data)
sudo -u www-data curl http://127.0.0.1:3000/health

# Должен вернуть JSON ответ
```

---

## Возможные проблемы и решения

### Проблема 1: Конфигурация не в sites-enabled

**Решение:**
```bash
# Создайте симлинк
ln -s /etc/nginx/sites-available/efflow-m50.com /etc/nginx/sites-enabled/

# Проверьте синтаксис
nginx -t

# Перезагрузите nginx
systemctl reload nginx
```

### Проблема 2: Конфликт с default конфигурацией

**Решение:**
```bash
# Удалите default конфигурацию если она конфликтует
rm /etc/nginx/sites-enabled/default

# Перезагрузите nginx
systemctl reload nginx
```

### Проблема 3: Nginx не может подключиться к Node.js

**Решение:**
```bash
# Проверьте, что Node.js слушает на 127.0.0.1:3000
ss -tlnp | grep :3000

# Должно быть: LISTEN ... 127.0.0.1:3000
```

### Проблема 4: Файрвол блокирует порт 80

**Решение:**
```bash
# Ubuntu/Debian
ufw allow 80/tcp
ufw reload

# CentOS/RHEL
firewall-cmd --permanent --add-service=http
firewall-cmd --reload
```

---

## Быстрое исправление

Если ничего не помогает, пересоздайте конфигурацию:

```bash
# 1. Удалите старую конфигурацию
rm /etc/nginx/sites-enabled/efflow-m50.com

# 2. Скопируйте конфигурацию из проекта
cp /root/reverse-proxy/nginx.conf /etc/nginx/sites-available/efflow-m50.com

# 3. Создайте симлинк
ln -s /etc/nginx/sites-available/efflow-m50.com /etc/nginx/sites-enabled/

# 4. Проверьте синтаксис
nginx -t

# 5. Перезагрузите nginx
systemctl reload nginx

# 6. Проверьте работу
curl -I http://efflow-m50.com
```
