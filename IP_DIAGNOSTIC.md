# 🔍 Диагностика IP адресов

## Проблема
- DNS указывает на `83.217.208.92`
- Но запрос идет с `83.217.208.93` на `83.217.208.92:80`
- Соединение отклоняется

## Проверка на сервере

### 1. Проверьте все IP адреса сервера:
```bash
ip addr show
# Или
hostname -I
```

### 2. Проверьте, на каком IP слушает nginx:
```bash
ss -tlnp | grep :80
# Должно показать на каком IP слушает nginx
```

### 3. Проверьте, может ли сервер подключиться к самому себе по IP 92:
```bash
# С сервера попробуйте подключиться к IP 92
curl -v http://83.217.208.92

# Если не работает - проблема в сетевой конфигурации
```

### 4. Проверьте локальное подключение:
```bash
curl -v http://127.0.0.1
# Должен работать
```

### 5. Проверьте, какой IP у сервера на самом деле:
```bash
# Внешний IP
curl ifconfig.me

# Все IP адреса
ip addr | grep "inet "
```

---

## Возможные проблемы

### Проблема 1: У сервера два IP, но nginx не слушает на нужном
**Решение:** Явно укажите IP в конфигурации nginx

### Проблема 2: Проблема с маршрутизацией между IP
**Решение:** Используйте localhost для проксирования

### Проблема 3: Файрвол блокирует соединение между IP
**Решение:** Проверьте правила файрвола

---

## Важно
Нужно понять:
- Сколько IP адресов у сервера?
- На каком IP должен работать сайт?
- Почему запрос идет с IP 93 на IP 92?
