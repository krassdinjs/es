# 🔍 Проверка IP 83.217.208.93

## Проблема
DNS указывает на `83.217.208.93`, но соединение отклоняется.

## Проверка на сервере

### 1. Проверьте, есть ли у сервера IP 93:
```bash
ip addr show | grep "83.217.208.93"
# Или
ip addr | grep "inet.*83.217.208.93"
```

### 2. Проверьте все IP адреса сервера:
```bash
ip addr show
hostname -I
```

### 3. Проверьте, слушает ли nginx на IP 93:
```bash
ss -tlnp | grep :80
# Должно показать на каких IP слушает nginx
```

### 4. Попробуйте подключиться к IP 93 напрямую:
```bash
curl -v http://83.217.208.93
# Если не работает - проблема в сетевой конфигурации
```

### 5. Проверьте, может ли сервер подключиться к самому себе по IP 93:
```bash
# С сервера
curl -v http://83.217.208.93
```

### 6. Проверьте логи nginx:
```bash
tail -f /var/log/nginx/error.log
# В другом терминале попробуйте:
curl http://83.217.208.93
```

---

## Решения

### Если IP 93 НЕ привязан к серверу:
Нужно привязать IP 93 к сетевому интерфейсу (обычно делается в панели хостинга).

### Если IP 93 привязан, но nginx не отвечает:
Проверьте конфигурацию nginx - возможно нужно явно указать IP.

### Если проблема в маршрутизации:
Проверьте сетевые настройки и файрвол.

---

## Быстрая проверка

```bash
# 1. Есть ли IP 93?
ip addr | grep 93

# 2. Слушает ли nginx?
ss -tlnp | grep :80

# 3. Работает ли локально?
curl http://127.0.0.1

# 4. Работает ли по IP 93?
curl http://83.217.208.93
```
