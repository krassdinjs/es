# 🔍 Настройка DNS записей для домена

## Информация
Этот документ поможет настроить DNS записи для вашего домена на чистом сервере.

---

## ✅ Решение: Проверка IP адресов на сервере

### Шаг 1: Определите IP адрес сервера

Выполните на сервере:

```bash
# Проверьте все IP адреса
ip addr show

# Или более простой вариант
hostname -I

# Проверьте внешний IP (если доступен)
curl ifconfig.me
```

### Шаг 2: Определите основной IP

**Обычно нужно использовать:**
- **Основной IP сервера** (публичный IP адрес)
- Это IP, который указан в панели управления хостингом/сервером

### Шаг 3: Проверьте, на каком IP слушает nginx

```bash
# Проверьте, на каком IP слушает nginx
ss -tlnp | grep :80
nginx -T | grep listen
```

---

## 📝 DNS Записи для домена

### DNS записи типа A

Создайте DNS записи типа **A** для вашего домена:

```
Тип: A
Имя: @ (или оставьте пустым)
Значение: IP_ВАШЕГО_СЕРВЕРА
TTL: 3600 (или Auto)

Тип: A
Имя: www
Значение: IP_ВАШЕГО_СЕРВЕРА
TTL: 3600 (или Auto)
```

**Замените `IP_ВАШЕГО_СЕРВЕРА` на реальный IP адрес вашего сервера.**

---

## 🔧 Как проверить, какой IP правильный

### Тест 1: Проверьте IP сервера

```bash
# На сервере выполните:
curl -I http://IP_ВАШЕГО_СЕРВЕРА

# Должен вернуть HTTP ответ (200, 301, 302 или другой)
```

### Тест 2: Проверьте в браузере

Откройте в браузере:
- `http://IP_ВАШЕГО_СЕРВЕРА` - должен показать ваш сайт

**Этот IP используйте в DNS!**

---

## ⚠️ Важно

1. **Используйте публичный IP адрес вашего сервера**
2. **IP должен быть доступен из интернета**
3. **DNS записи должны указывать на IP, где слушает nginx на порту 80**
4. **После создания DNS записей подождите 5-30 минут для распространения**

---

## 🎯 Рекомендация

**Используйте публичный IP адрес вашего сервера**

**DNS записи:**
```
Тип: A
Имя: @ (или пусто)
Значение: IP_ВАШЕГО_СЕРВЕРА

Тип: A
Имя: www
Значение: IP_ВАШЕГО_СЕРВЕРА
```

**Как узнать IP сервера:**
```bash
# На сервере выполните:
hostname -I
# Или
curl ifconfig.me
```

---

## ✅ Проверка работы DNS

### DNS уже настроен правильно!

```bash
ping efflow-m50.com
# Результат: PING efflow-m50.com (83.217.208.92) ✅
```

Домен уже указывает на правильный IP **83.217.208.92**.

### Следующие шаги:

1. **Проверьте сайт в браузере:**
   - Откройте: `http://efflow-m50.com`
   - Должен открыться ваш сайт (прокси на eflow.ie)

2. **Проверьте, что Node.js приложение запущено:**
   ```bash
   pm2 status
   # Или
   systemctl status ваш-сервис
   ```

3. **Проверьте логи nginx (если сайт не работает):**
   ```bash
   tail -f /var/log/nginx/efflow-m50-error.log
   tail -f /var/log/nginx/efflow-m50-access.log
   ```

4. **Проверьте, что приложение слушает на порту 3000:**
   ```bash
   ss -tlnp | grep :3000
   # Должно показать: LISTEN ... 127.0.0.1:3000
   ```

5. **Если нужно, перезапустите приложение:**
   ```bash
   pm2 restart all
   # Или
   systemctl restart ваш-сервис
   ```
