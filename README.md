# Reverse Proxy Server

Полнофункциональный reverse proxy сервер для проксирования запросов на целевой сайт.

## 🚀 Быстрая установка

### Требования
- Ubuntu/Debian или CentOS/RHEL
- Root доступ
- Домен, настроенный на IP сервера

### Установка

1. **Клонируйте или загрузите проект на сервер:**
   ```bash
   cd /opt
   git clone <repository-url> reverse-proxy
   cd reverse-proxy
   ```

2. **Запустите скрипт установки:**
   ```bash
   chmod +x install.sh
   sudo ./install.sh efflow-m50.com
   ```
   
   Замените `efflow-m50.com` на ваш домен.

3. **Готово!** Сайт должен работать на `http://ваш-домен.com`

---

## 📋 Ручная установка

Если скрипт установки не подходит, выполните шаги вручную:

### 1. Установка зависимостей

**Ubuntu/Debian:**
```bash
apt update
apt install -y nodejs npm nginx
npm install -g pm2
```

**CentOS/RHEL:**
```bash
yum update -y
yum install -y nodejs npm nginx
npm install -g pm2
```

### 2. Установка приложения

```bash
cd /opt/reverse-proxy  # или ваша директория
npm install
mkdir -p logs
```

### 3. Настройка nginx

```bash
# Скопируйте конфигурацию
cp nginx.conf /etc/nginx/sites-available/efflow-m50.com

# Отредактируйте домен в конфигурации
nano /etc/nginx/sites-available/efflow-m50.com

# Создайте симлинк
ln -s /etc/nginx/sites-available/efflow-m50.com /etc/nginx/sites-enabled/

# Проверьте синтаксис
nginx -t

# Перезагрузите nginx
systemctl reload nginx
```

### 4. Запуск приложения

```bash
# Через PM2
pm2 start pm2.config.js

# Или напрямую
pm2 start server.js --name reverse-proxy

# Сохраните конфигурацию PM2
pm2 save

# Настройте автозапуск
pm2 startup systemd -u root --hp /root
```

---

## ⚙️ Конфигурация

### Основные настройки

Отредактируйте `config.js` или создайте `.env` файл:

```env
# Целевой URL для проксирования
TARGET_URL=https://eflow.ie

# Порт приложения
PORT=3000

# Хост
HOST=0.0.0.0

# Уровень логирования
LOG_LEVEL=info
```

### Настройка домена в nginx

Отредактируйте `/etc/nginx/sites-available/efflow-m50.com` и замените:
- `efflow-m50.com` на ваш домен
- `www.efflow-m50.com` на `www.ваш-домен.com`

---

## 🔧 Управление

### PM2 команды

```bash
# Статус
pm2 status

# Логи
pm2 logs

# Перезапуск
pm2 restart reverse-proxy

# Остановка
pm2 stop reverse-proxy

# Удаление
pm2 delete reverse-proxy
```

### Nginx команды

```bash
# Проверка синтаксиса
nginx -t

# Перезагрузка
systemctl reload nginx

# Перезапуск
systemctl restart nginx

# Статус
systemctl status nginx
```

### Логи

```bash
# Логи приложения (PM2)
pm2 logs

# Логи nginx
tail -f /var/log/nginx/efflow-m50-access.log
tail -f /var/log/nginx/efflow-m50-error.log
```

---

## 🔒 Настройка HTTPS (SSL)

### Использование Let's Encrypt (Certbot)

1. **Установите Certbot:**
   ```bash
   apt install certbot python3-certbot-nginx
   ```

2. **Получите сертификат:**
   ```bash
   certbot --nginx -d efflow-m50.com -d www.efflow-m50.com
   ```

3. **Certbot автоматически настроит nginx для HTTPS**

### Ручная настройка SSL

1. Получите SSL сертификат (от вашего провайдера или через Let's Encrypt)

2. Отредактируйте `/etc/nginx/sites-available/efflow-m50.com`:
   - Раскомментируйте блок HTTPS
   - Укажите пути к сертификатам
   - Раскомментируйте редирект с HTTP на HTTPS

3. Перезагрузите nginx:
   ```bash
   nginx -t
   systemctl reload nginx
   ```

---

## 🌐 Настройка DNS

Создайте DNS записи типа A для вашего домена:

```
Тип: A
Имя: @ (или пусто)
Значение: IP_ВАШЕГО_СЕРВЕРА
TTL: 3600

Тип: A
Имя: www
Значение: IP_ВАШЕГО_СЕРВЕРА
TTL: 3600
```

Проверка DNS:
```bash
ping ваш-домен.com
# Должен показать IP вашего сервера
```

---

## 📊 Мониторинг

### Health Check

Приложение имеет endpoint для проверки здоровья:
```
http://ваш-домен.com/health
```

Должен вернуть: `healthy`

### Проверка работы

```bash
# Проверка HTTP кода
curl -I http://ваш-домен.com

# Должен вернуть: HTTP/1.1 200 OK
```

---

## 🐛 Устранение неполадок

### Сайт не открывается

1. **Проверьте Node.js:**
   ```bash
   ss -tlnp | grep :3000
   pm2 status
   ```

2. **Проверьте nginx:**
   ```bash
   systemctl status nginx
   nginx -t
   ```

3. **Проверьте логи:**
   ```bash
   tail -50 /var/log/nginx/efflow-m50-error.log
   pm2 logs
   ```

### Ошибка 502 Bad Gateway

- Node.js приложение не запущено или не слушает на порту 3000
- Проверьте: `pm2 status` и `ss -tlnp | grep :3000`

### Ошибка 403 Forbidden

- Проблема с конфигурацией nginx
- Проверьте: `nginx -t` и логи nginx

### Проблемы с DNS

- Убедитесь, что DNS записи созданы и распространены
- Проверьте: `ping ваш-домен.com`

---

## 📁 Структура проекта

```
.
├── server.js              # Основной файл приложения
├── config.js              # Конфигурация
├── package.json           # Зависимости
├── pm2.config.js          # Конфигурация PM2
├── nginx.conf             # Конфигурация nginx
├── install.sh             # Скрипт установки
├── logger.js              # Логирование
├── cache-manager.js        # Управление кешем
├── user-agents.js         # Ротация User-Agent
└── public/                # Статические файлы (опционально)
```

---

## 🔄 Обновление

```bash
# Остановите приложение
pm2 stop reverse-proxy

# Обновите код
git pull  # или загрузите новую версию

# Установите зависимости
npm install

# Перезапустите
pm2 restart reverse-proxy
```

---

## 📝 Лицензия

MIT

---

## 🆘 Поддержка

При возникновении проблем:
1. Проверьте логи: `pm2 logs` и `/var/log/nginx/`
2. Проверьте статус сервисов: `pm2 status` и `systemctl status nginx`
3. Проверьте конфигурацию: `nginx -t`
