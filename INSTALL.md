# 🚀 Установка на чистый сервер

## Быстрая установка

```bash
# 1. Загрузите проект на сервер
cd /opt
git clone <repository-url> reverse-proxy
cd reverse-proxy

# 2. Запустите скрипт установки
chmod +x install.sh
sudo ./install.sh efflow-m50.com
```

**Готово!** Сайт должен работать на `http://efflow-m50.com`

---

## Что делает скрипт установки

1. ✅ Устанавливает Node.js, PM2, nginx
2. ✅ Устанавливает npm зависимости
3. ✅ Настраивает nginx конфигурацию
4. ✅ Запускает приложение через PM2
5. ✅ Настраивает автозапуск

---

## Ручная установка

Если скрипт не подходит, см. `README.md` для подробных инструкций.

---

## После установки

1. **Настройте DNS** - см. `DNS_SETUP.md`
2. **Настройте SSL** (опционально) - см. `README.md`
3. **Проверьте работу:**
   ```bash
   curl -I http://ваш-домен.com
   ```

---

## Полезные команды

```bash
# PM2
pm2 status
pm2 logs
pm2 restart reverse-proxy

# Nginx
systemctl status nginx
nginx -t
systemctl reload nginx

# Логи
tail -f /var/log/nginx/efflow-m50-access.log
pm2 logs
```
