# 🚂 Railway Setup Guide

## Рекомендуемые настройки для минимизации рисков

### 1. Environment Variables (Variables tab)

```env
# ОБЯЗАТЕЛЬНЫЕ
TARGET_URL=https://eflow.ie
NODE_ENV=production
PORT=8080

# РЕКОМЕНДУЕМЫЕ
LOG_LEVEL=warn
LOG_REQUESTS=false
ENABLE_WEBSOCKET=true
ENABLE_COMPRESSION=true
PROXY_TIMEOUT=30000
TRUST_PROXY=true
```

### 2. Regions (Settings → Deploy)

**✅ ВЫБРАТЬ ТОЛЬКО ОДИН:**
- EU West (Amsterdam) - ближе к Ирландии
- Или US East (Virginia) - если больше трафика из США

**❌ НЕ использовать несколько регионов!**

### 3. Resources (Settings → Deploy)

```
Memory: 512 MB - 1 GB (не больше!)
vCPU: 0.5 - 1.0
```

### 4. Healthcheck (Settings → Deploy)

```
Healthcheck Path: /health
Healthcheck Timeout: 300 seconds
```

### 5. Restart Policy (Settings → Deploy)

```
Type: On Failure
Max Retries: 10
```

### 6. Custom Domain (Settings → Networking)

**НАСТОЯТЕЛЬНО РЕКОМЕНДУЕТСЯ!**

Причины:
- Railway домены легко блокируются
- Свой домен выглядит легитимно
- Можно быстро сменить при проблемах

Где купить дешево:
- Namecheap (~$1-3/год .xyz/.site)
- Cloudflare Registrar (по себестоимости)
- Porkbun (~$1-5/год)

### 7. Что НЕ делать:

❌ НЕ включать Serverless
❌ НЕ использовать Static IPs (привлекает внимание)
❌ НЕ деплоить в несколько регионов
❌ НЕ ставить много памяти/CPU
❌ НЕ логировать все подряд (LOG_REQUESTS=false в проде)

### 8. Дополнительная защита

**Cloudflare перед Railway:**
1. Домен → Cloudflare DNS
2. Cloudflare Proxy (оранжевое облако)
3. Railway как origin server
4. Получаете Cloudflare IP вместо Railway

**Преимущества:**
- DDoS защита
- Дополнительный кэш
- Скрытие реального IP Railway
- Web Application Firewall

### 9. Мониторинг

Проверяйте регулярно:
- Логи Railway (не должно быть массовых ошибок)
- Использование ресурсов (должно быть стабильным)
- Uptime (должен быть >99%)

### 10. План действий при блокировке

Если заблокировали:

**Вариант А - Быстрый:**
1. Смените Custom Domain
2. Или смените Railway проект (новый URL)

**Вариант Б - Через Cloudflare:**
1. Добавьте Cloudflare перед Railway
2. Включите их защиту
3. Используйте Cloudflare Workers для дополнительного слоя

**Вариант В - Другой хостинг:**
- Render.com
- Fly.io
- Heroku
- DigitalOcean App Platform

---

## ⚡ Быстрая проверка после деплоя:

```bash
# 1. Проверьте health
curl https://your-domain.railway.app/health

# 2. Проверьте основной сайт
curl -I https://your-domain.railway.app/

# 3. Проверьте кэширование
curl -I https://your-domain.railway.app/ | grep X-Cache

# 4. Проверьте User-Agent (должен быть случайный)
```

## 📊 Нормальные показатели:

```
Requests/min: 10-30 (не больше!)
Memory usage: 100-300 MB
CPU usage: 5-15%
Cache hit rate: 70-85%
Uptime: >99%
```

Если показатели выше - снизьте нагрузку или траффик привлечет внимание.

