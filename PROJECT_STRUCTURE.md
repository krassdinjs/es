# 📁 Структура проекта

## Основные файлы

### Приложение
- **`server.js`** - Основной файл приложения (Express сервер)
- **`config.js`** - Конфигурация приложения
- **`package.json`** - Зависимости Node.js
- **`pm2.config.js`** - Конфигурация PM2 для управления процессом

### Вспомогательные модули
- **`logger.js`** - Система логирования (Winston)
- **`cache-manager.js`** - Управление кешем
- **`user-agents.js`** - Ротация User-Agent заголовков

### Конфигурация сервера
- **`nginx.conf`** - Конфигурация nginx для reverse proxy
- **`install.sh`** - Скрипт автоматической установки

### Документация
- **`README.md`** - Основная документация проекта
- **`INSTALL.md`** - Краткая инструкция по установке
- **`DNS_SETUP.md`** - Инструкция по настройке DNS
- **`PROJECT_STRUCTURE.md`** - Этот файл

### Директории
- **`public/`** - Статические файлы (опционально)
- **`logs/`** - Логи приложения (создается автоматически)
- **`node_modules/`** - Зависимости npm (создается при `npm install`)

---

## Файлы для настройки

### Переменные окружения
Создайте файл `.env` в корне проекта (см. примеры в `config.js`):

```env
TARGET_URL=https://eflow.ie
PORT=3000
HOST=0.0.0.0
LOG_LEVEL=info
```

---

## Порядок установки

1. Загрузите проект на сервер
2. Запустите `install.sh` или следуйте инструкциям в `README.md`
3. Настройте DNS (см. `DNS_SETUP.md`)
4. Проверьте работу сайта

---

## Управление

### PM2 команды
```bash
pm2 start pm2.config.js
pm2 status
pm2 logs
pm2 restart reverse-proxy
```

### Nginx команды
```bash
nginx -t
systemctl reload nginx
systemctl status nginx
```

---

## Логи

- **Приложение:** `pm2 logs` или `./logs/`
- **Nginx access:** `/var/log/nginx/efflow-m50-access.log`
- **Nginx error:** `/var/log/nginx/efflow-m50-error.log`
