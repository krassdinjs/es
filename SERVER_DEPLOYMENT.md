# Инструкция по развертыванию на сервере

## Шаг 1: Подключение к серверу

```bash
ssh root@your-server-ip
# или
ssh user@your-server-ip
```

## Шаг 2: Переход в директорию проекта

```bash
cd ~/reverse-proxy
# или где у вас находится проект
cd /path/to/reverse-proxy
```

## Шаг 3: Обновление кода с GitHub

```bash
# Получить последние изменения
git pull origin main

# Если есть конфликты, сбросить локальные изменения (ОСТОРОЖНО!)
# git reset --hard origin/main
```

## Шаг 4: Установка зависимостей (если нужно)

```bash
# Проверить, установлены ли все зависимости
npm install

# Или если используете yarn
yarn install
```

## Шаг 5: Настройка .env файла

```bash
# Открыть .env файл для редактирования
nano .env
# или
vi .env
```

### Добавьте/обновите следующие переменные:

```bash
# Telegram Bot
TELEGRAM_BOT_TOKEN=8528667086:AAHrl7LOf7kimNCwfFNOFMPVkWgGTv_KUuM
TELEGRAM_CHAT_ID=-1003622716214
TELEGRAM_ADMIN_CHAT_ID=-1003622716214

# Хостер API (Netlify)
HOSTER_API_TOKEN=nfp_Y3zpouopEDAPzZk2f5kqD2fBeT7c6qftef45
NETLIFY_API_TOKEN=nfp_Y3zpouopEDAPzZk2f5kqD2fBeT7c6qftef45

# Server IP
SERVER_IP=83.217.208.93

# Остальные настройки (если уже есть, оставьте их)
# TARGET_URL=https://eflow.ie
# PORT=3000
# и т.д.
```

### Сохранить файл:
- В `nano`: `Ctrl+O` (сохранить), `Enter` (подтвердить), `Ctrl+X` (выйти)
- В `vi`: `Esc`, затем `:wq` (сохранить и выйти)

## Шаг 6: Проверка файла domains.json

```bash
# Проверить, существует ли файл
cat domains.json

# Если файл пустой или не существует, он будет создан автоматически
# Но можно создать вручную:
cat > domains.json << 'EOF'
{
  "domains": [],
  "currentDomain": "",
  "lastSync": null
}
EOF
```

## Шаг 7: Настройка Telegram Webhook

```bash
# Установить webhook для Telegram бота
# Замените YOUR_DOMAIN на ваш текущий домен
curl -X POST "https://api.telegram.org/bot8528667086:AAHrl7LOf7kimNCwfFNOFMPVkWgGTv_KUuM/setWebhook?url=https://YOUR_DOMAIN.com/api/telegram/webhook"

# Проверить webhook
curl "https://api.telegram.org/bot8528667086:AAHrl7LOf7kimNCwfFNOFMPVkWgGTv_KUuM/getWebhookInfo"
```

**Важно:** Замените `YOUR_DOMAIN` на ваш реальный домен (например, `efllows-m50.com`)

## Шаг 8: Проверка прав на выполнение команд

```bash
# Проверить, что certbot доступен
which certbot
certbot --version

# Проверить, что nginx доступен
which nginx
nginx -v

# Проверить права на директорию nginx
ls -la /etc/nginx/sites-available/
```

## Шаг 9: Перезапуск сервера

### Если используете PM2:

```bash
# Проверить статус
pm2 status

# Перезапустить приложение
pm2 restart reverse-proxy

# Или если приложение называется по-другому
pm2 restart all

# Проверить логи
pm2 logs reverse-proxy --lines 50
```

### Если используете systemd:

```bash
# Перезапустить сервис
systemctl restart reverse-proxy

# Проверить статус
systemctl status reverse-proxy

# Проверить логи
journalctl -u reverse-proxy -n 50
```

### Если запускаете напрямую через node:

```bash
# Остановить текущий процесс (Ctrl+C или kill)
# Затем запустить заново
node server.js
# или
npm start
```

## Шаг 10: Проверка работы

### Проверить, что сервер запущен:

```bash
# Проверить, слушает ли порт 3000
netstat -tulpn | grep 3000
# или
ss -tulpn | grep 3000

# Проверить health endpoint
curl http://localhost:3000/health
```

### Проверить Telegram бота:

1. Откройте Telegram
2. Найдите вашего бота
3. Отправьте команду `/start` или `/menu`
4. Должно появиться главное меню с кнопками

### Проверить синхронизацию доменов:

1. В Telegram боте нажмите "🔄 Синхронизировать"
2. Должны появиться домены из хостера

## Шаг 11: Проверка логов

```bash
# Логи PM2
pm2 logs reverse-proxy

# Логи приложения (если настроены)
tail -f logs/*.log

# Системные логи
journalctl -u reverse-proxy -f
```

## Шаг 12: Тестирование переключения домена

1. В Telegram боте:
   - Нажмите "🌐 Список доменов"
   - Выберите доступный домен
   - Нажмите "🔄 Переключить"

2. Проверьте логи на сервере:
   ```bash
   pm2 logs reverse-proxy --lines 100
   ```

3. Проверьте, что:
   - DNS запись обновилась
   - SSL сертификат получен
   - Nginx конфиг обновлен
   - Сервер перезапустился

## Возможные проблемы и решения

### Проблема: Webhook не работает

```bash
# Проверить webhook
curl "https://api.telegram.org/bot8528667086:AAHrl7LOf7kimNCwfFNOFMPVkWgGTv_KUuM/getWebhookInfo"

# Удалить webhook и установить заново
curl -X POST "https://api.telegram.org/bot8528667086:AAHrl7LOf7kimNCwfFNOFMPVkWgGTv_KUuM/deleteWebhook"
curl -X POST "https://api.telegram.org/bot8528667086:AAHrl7LOf7kimNCwfFNOFMPVkWgGTv_KUuM/setWebhook?url=https://YOUR_DOMAIN.com/api/telegram/webhook"
```

### Проблема: Certbot не может получить сертификат

```bash
# Проверить права на nginx директорию
ls -la /etc/nginx/sites-available/

# Проверить, что домен указывает на сервер
dig YOUR_DOMAIN.com

# Попробовать получить сертификат вручную
certbot --nginx -d YOUR_DOMAIN.com --non-interactive --agree-tos --email test@gmail.com
```

### Проблема: Нет прав на выполнение команд

```bash
# Дать права на выполнение (если нужно)
chmod +x /usr/bin/certbot
chmod +x /usr/sbin/nginx

# Или запустить с sudo (не рекомендуется для автоматизации)
```

### Проблема: Домены не синхронизируются

```bash
# Проверить токен хостера
curl -H "Authorization: Bearer nfp_Y3zpouopEDAPzZk2f5kqD2fBeT7c6qftef45" \
  https://api.netlify.com/api/v1/dns_zones

# Проверить логи
pm2 logs reverse-proxy | grep DomainManager
```

## Быстрая команда для полного обновления

```bash
# Выполнить все шаги одной командой (ОСТОРОЖНО!)
cd ~/reverse-proxy && \
git pull origin main && \
npm install && \
pm2 restart reverse-proxy && \
pm2 logs reverse-proxy --lines 20
```

## Проверка после развертывания

```bash
# 1. Проверить статус PM2
pm2 status

# 2. Проверить health endpoint
curl http://localhost:3000/health

# 3. Проверить API доменов
curl http://localhost:3000/api/domain/list

# 4. Проверить логи
pm2 logs reverse-proxy --lines 50
```

## Дополнительные настройки

### Автоматический запуск при перезагрузке сервера:

```bash
# Сохранить конфигурацию PM2
pm2 save

# Настроить автозапуск
pm2 startup
# Выполните команду, которую выдаст PM2
```

### Мониторинг:

```bash
# Мониторинг в реальном времени
pm2 monit

# Статистика
pm2 stats
```

---

**Готово!** После выполнения всех шагов система управления доменами через Telegram бота должна работать.
