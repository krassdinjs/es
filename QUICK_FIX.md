# 🔧 Быстрое исправление ERR_CONNECTION_CLOSED

## Проблема
- `curl http://127.0.0.1:3000` не отвечает (пустой ответ)
- `curl http://efflow-m50.com` не может подключиться

## Диагностика на сервере

Выполните эти команды по порядку:

### 1. Проверьте, что приложение действительно запущено:
```bash
pm2 status
ss -tlnp | grep :3000
```

### 2. Проверьте логи в реальном времени:
```bash
pm2 logs reverse-proxy --lines 0
# В другом терминале выполните:
curl http://127.0.0.1:3000
# Смотрите что появляется в логах
```

### 3. Проверьте, может ли приложение подключиться к целевому URL:
```bash
curl -I https://eflow.ie
# Должен вернуть HTTP ответ
```

### 4. Проверьте nginx конфигурацию:
```bash
cat /etc/nginx/sites-available/efflow-m50.com
# Или
cat /etc/nginx/sites-enabled/efflow-m50.com
```

### 5. Проверьте nginx логи:
```bash
tail -f /var/log/nginx/efflow-m50-error.log
# В другом терминале попробуйте открыть сайт
```

### 6. Попробуйте запустить приложение напрямую (без PM2):
```bash
cd ~/reverse-proxy
node server.js
# Попробуйте curl http://127.0.0.1:3000 в другом терминале
# Смотрите что выводится в консоль
```

---

## Возможные проблемы и решения

### Проблема 1: Приложение не может подключиться к целевому URL
**Решение:** Проверьте интернет-соединение и доступность https://eflow.ie

### Проблема 2: Nginx не настроен правильно
**Решение:** Проверьте конфигурацию nginx и убедитесь, что proxy_pass указывает на 127.0.0.1:3000

### Проблема 3: Приложение падает при обработке запроса
**Решение:** Проверьте логи на ошибки, возможно проблема с зависимостями или конфигурацией

### Проблема 4: Файрвол блокирует порты
**Решение:** 
```bash
# Ubuntu/Debian
ufw allow 80/tcp
ufw allow 443/tcp
ufw allow 3000/tcp

# Проверьте статус
ufw status
```

---

## Быстрое решение

Если ничего не помогает, попробуйте:

```bash
# 1. Остановите все
pm2 stop reverse-proxy
systemctl stop nginx

# 2. Запустите приложение напрямую для теста
cd ~/reverse-proxy
node server.js

# 3. В другом терминале проверьте
curl http://127.0.0.1:3000

# Если работает - проблема в PM2 или nginx
# Если не работает - проблема в коде приложения
```
